# 环境变量配置示例
# 复制此文件为 .env.development 或 .env.production 并修改相应的值

# 部署域名（已部署应用的访问地址）
# 开发环境建议使用 /deploy 代理路径（配合 vite.config.ts 中的 proxy 规则），确保同源访问
# 生产环境需配置为实际的部署域名，并确保与前端同源或设置 CORS
VITE_APP_DEPLOY_BASE_URL=http://localhost:8080
